name: Conventional Commits Changelog
on:
  release:
    types: released

jobs:
  conventional-commits-changelog:
    uses: .github/workflows/TriPSs/conventional-changelog-action@v3
    with:
      github-token: ${{ secrets.github_token }}
